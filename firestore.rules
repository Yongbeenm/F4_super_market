rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow all read and write for testing
    // TODO: Update these rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
